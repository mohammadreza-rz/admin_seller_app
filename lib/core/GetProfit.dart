import 'package:http/http.dart' as http;

class Back {
  List GetProfit(DateTime startTime, DateTime endTime) {
    List result = new List();
    result.add(500);
    result.add(100);
    return result;
  }
}
